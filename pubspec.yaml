name: couple_money_app
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+4

environment:
  sdk: ">=2.12.0 <3.0.0"


dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

  freezed:
  flutter_state_notifier:
  build_runner:

  holding_gesture:

flutter:
  uses-material-design: true
